#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../aws-lambda/bin/lambda" "$@"
  ret=$?
else 
  node  "$basedir/../aws-lambda/bin/lambda" "$@"
  ret=$?
fi
exit $ret
